# — ROS2 / colcon 빌드 폴더
build/
install/
log/

# — Catkin(ROS1) 호환 (필요 없으면 삭제 가능)
devel/
devel_isolated/
build_isolated/
logs/

# — Python 바이트코드 캐시
__pycache__/
*.py[cod]
*$py.class

# — 에디터 & IDE 설정
.vscode/
.idea/

# — OS별 임시 파일
.DS_Store
Thumbs.db

# — 에디터 스왑·백업 파일
*.swp
*.bak
*.tmp

# — 기타
CATKIN_IGNORE
